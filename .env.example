# Maps JavaScript API key — never commit a real value.
# GitHub Pages: repo Settings → Secrets and variables → Actions → VITE_GOOGLE_MAPS_API_KEY
# Local: copy to .env.local
VITE_GOOGLE_MAPS_API_KEY=
